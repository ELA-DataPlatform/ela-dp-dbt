extends: default

rules:
  line-length:
    max: 150
    allow-non-breakable-inline-mappings: true
  truthy:
    check-keys: false
  document-start: disable
  indentation:
    spaces: 2
    indent-sequences: true
